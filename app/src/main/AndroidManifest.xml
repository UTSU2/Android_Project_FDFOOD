<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FDFOOD"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddActivity" android:label="맛집 추가" />
        <activity android:name=".RecommendActivity" android:label="맛집 추천" />
        <activity android:name=".RemoveActivity" android:label="맛집 검색" />
        <activity android:name=".KoreaActivity" android:label="한식" />
        <activity android:name=".JapanActivity" android:label="일식" />
        <activity android:name=".ChinaActivity" android:label="중식" />
        <activity android:name=".WesternActivity" android:label="양식" />
        <activity android:name=".OthersActivity" android:label="기타" />
    </application>

</manifest>